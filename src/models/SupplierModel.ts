export interface SupplierModel {
  name: string;
  slug: string;
  product: string;
  categorires: String[];
  price: number;
  contact: string;
  isTaking: boolean;
  photoUrl: string;
  _id: string;
  createdAt: string;
  email: string;
  active: number;
}
